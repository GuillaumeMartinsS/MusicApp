{% extends 'base.html.twig' %}

{% block title %}Edit Chanson{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Chanson</h1>

        <a class="btn btn-info" href="{{ path('back_song_index') }}">Back to list</a>

        {{ include('back_song/_edit_form.html.twig', {'button_label': 'Update'}) }}
    
        {{ include('back_song/_delete_form.html.twig') }}
    </div>
{% endblock %}
