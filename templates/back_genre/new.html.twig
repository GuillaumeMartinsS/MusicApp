{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Genre</h1>

        <a class="btn btn-info" href="{{ path('back_genre_index') }}">Back to list</a>

        {{ include('back_genre/_new_form.html.twig') }}

    </div>
{% endblock %}