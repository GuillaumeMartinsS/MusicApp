{% extends 'base.html.twig' %}

{% block title %}Edit Genre{% endblock %}

{% block body %}
    <div class="container">

        <h1>Edit Genre</h1>

        <a class="btn btn-info" href="{{ path('back_genre_index') }}">Back to list</a>

        {{ include('back_genre/_edit_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('back_genre/_delete_form.html.twig') }}
    </div>
{% endblock %}
