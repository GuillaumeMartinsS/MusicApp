{% extends 'base.html.twig' %}

{% block title %}Banner{% endblock %}

{% block body %}
    <h1>Banner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ banner.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ banner.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ banner.createdAt ? banner.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ banner.updatedAt ? banner.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_banner_index') }}">back to list</a>

    <a href="{{ path('app_back_banner_edit', {'id': banner.id}) }}">edit</a>

    {{ include('back_banner/_delete_form.html.twig') }}
{% endblock %}
