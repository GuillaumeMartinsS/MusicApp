{% extends 'base.html.twig' %}

{% block title %}Edit Banner{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Banner</h1>

        <a class="btn btn-info" href="{{ path('back_banner_index') }}">Back to list</a>

        {{ include('back_banner/_edit_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('back_banner/_delete_form.html.twig') }}
    </div>
{% endblock %}




