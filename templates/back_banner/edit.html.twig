{% extends 'base.html.twig' %}

{% block title %}Edit Banner{% endblock %}

{% block body %}
    <h1>Edit Banner</h1>

    {{ include('back_banner/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_banner_index') }}">back to list</a>

    {{ include('back_banner/_delete_form.html.twig') }}
{% endblock %}
