{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit User</h1>

        <a class="btn btn-info" href="{{ path('back_user_index') }}">Back to list</a>

        {{ include('back_user/_edit_form.html.twig', {'button_label': 'Update'}) }}
        
        {{ include('back_user/_delete_form.html.twig') }}

    </div>
{% endblock %}
